---
"@palomapf-dev/pf-field-core": patch
---

ドキュメントを 0.7.0 の実装に合わせる（コードの変更なし）。

**README** — 「M0 完了」のままで、IndexedDB キュー以降が全部これから着手するように
見えていた。初見の担当者が最初に読む場所なので、実装状況の表を実物に置き換えた。
未確定の項目（実 Supabase の疎通・滞留上限・圧縮の実機実測）と、
1.0.0 を出さない理由も明示。

**integration-nextjs.md** — **組み込みの節が丸ごと無かった**。
依存の追加と環境変数までしか書いておらず、「で、どう繋ぐのか」が載っていない。
Route Handler 2本・`FieldCoreProvider`・`worker/sw.ts` を追加した（型と突き合わせ済み）。
401 と 403 の導線の出し分けも明記している。M8 の横展開でここが入口になる。

**DESIGN.md** — 「実装前レビュー用」の表題と、設計時のディレクトリ図・API 名が
実物と食い違っていた（`createSignUploadRouteHandler` など、写すと import が通らない）。
設計判断の記録であることを明記し、対応表とツリーの実物を入れた。

**auth-findings.md** — 調査時点のスナップショットとして本文は残し、
以降の実装で覆った結論（送信トークン導入・403 not_entitled）を冒頭に追記。

**滞留上限** — `DEFAULT_RETENTION`（300枚/120MB）と `core.ts`（200枚/160MB）が
根拠の違う2つの見積りとして並んでいる。値は動かさず、経路ごとの実効値と
「実測後に両方を揃えて確定する」ことを4箇所に明記した（0.7.0 で対応済み）。
